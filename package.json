{
  "name": "sahko-website",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "predev": "npm run build:client",
    "dev": "astro dev",
    "prebuild": "npm run build:client",
    "build": "astro build",
    "build:client": "esbuild src/site.client.js --minify --outfile=public/site.js",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "astro": "^7.1.3"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}